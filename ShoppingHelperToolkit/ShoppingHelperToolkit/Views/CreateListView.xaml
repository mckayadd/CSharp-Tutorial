<UserControl x:Class="ShoppingHelperToolkit.Views.CreateListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Input area -->
        <GroupBox Header="Add item" Padding="10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="220"/>
                    <ColumnDefinition Width="20"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="120"/>
                    <ColumnDefinition Width="20"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock Text="Name:" VerticalAlignment="Center"/>
                <TextBox Grid.Column="1"
                         Text="{Binding ItemName, UpdateSourceTrigger=PropertyChanged}" />

                <TextBlock Grid.Column="3" Text="Price:" VerticalAlignment="Center"/>
                <TextBox Grid.Column="4"
                         Text="{Binding PriceText, UpdateSourceTrigger=PropertyChanged}" />

                <Button Grid.Column="6"
                        Content="Add Item"
                        Command="{Binding AddItemCommand}"
                        Padding="10,4"/>
            </Grid>
        </GroupBox>

        <!-- Items list -->
        <GroupBox Grid.Row="2" Header="Current items" Padding="10" Margin="0,10,0,0">
            <ListView ItemsSource="{Binding CurrentItems}">
                <ListView.View>
                    <GridView>
                        <GridViewColumn Header="Item" DisplayMemberBinding="{Binding Name}" Width="240"/>
                        <GridViewColumn Header="Price" DisplayMemberBinding="{Binding Price}" Width="120"/>
                    </GridView>
                </ListView.View>
            </ListView>
        </GroupBox>

        <!-- Bottom actions -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" Margin="0,10,0,0">
            <Button Content="Save Shopping"
                    Command="{Binding SaveShoppingCommand}"
                    Padding="10,4"/>
            <TextBlock Text="{Binding StatusMessage}" Margin="12,0,0,0" VerticalAlignment="Center"/>
        </StackPanel>
    </Grid>
</UserControl>
