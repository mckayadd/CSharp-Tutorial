<UserControl x:Class="ShoppingHelperToolkit.Views.CreateListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="12"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Input form -->
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="220"/>
                <ColumnDefinition Width="12"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="120"/>
                <ColumnDefinition Width="12"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBlock Text="Item:" VerticalAlignment="Center"/>
            <TextBox Grid.Column="1"
                     Text="{Binding NewItemName, UpdateSourceTrigger=PropertyChanged}"
                     Margin="6,0,0,0"/>

            <TextBlock Grid.Column="3" Text="Price:" VerticalAlignment="Center"/>
            <TextBox Grid.Column="4"
                     Text="{Binding NewItemPrice, UpdateSourceTrigger=PropertyChanged}"
                     Margin="6,0,0,0"/>

            <Button Grid.Column="6"
                    Content="Add Item"
                    Command="{Binding AddItemCommand}"
                    Margin="6,0,0,0"
                    Width="90"/>
        </Grid>

        <!-- Save/Clear -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,10,0,0">
            <Button Content="Save Shopping"
                    Command="{Binding SaveShoppingCommand}"
                    Width="120"/>

            <Button Content="Clear"
                    Command="{Binding ClearCurrentCommand}"
                    Margin="10,0,0,0"
                    Width="80"/>
        </StackPanel>

        <!-- List -->
        <GroupBox Grid.Row="3" Header="Current items" Margin="0,10,0,0">
            <ListView ItemsSource="{Binding CurrentItems}">
                <ListView.View>
                    <GridView>
                        <GridViewColumn Header="Item" DisplayMemberBinding="{Binding Name}" Width="220"/>
                        <GridViewColumn Header="Price" DisplayMemberBinding="{Binding Price}" Width="120"/>
                    </GridView>
                </ListView.View>
            </ListView>
        </GroupBox>
    </Grid>
</UserControl>
