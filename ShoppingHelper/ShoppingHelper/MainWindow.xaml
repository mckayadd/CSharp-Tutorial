<Window x:Class="ShoppingHelper.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:ShoppingHelper.ViewModels"
        mc:Ignorable="d"
        Title="{Binding Title}" Height="450" Width="800">

    <Window.DataContext>
        <vm:MainViewModel/>
    </Window.DataContext>

    <Grid Margin="12">
        <TabControl>
            <TabItem Header="Create List">
                <Grid Margin="10">
                    <StackPanel>
                        <TextBlock Text="Current shopping list items:" FontWeight="Bold" />
                        <ListView ItemsSource="{Binding CreateList.CurrentItems}" Height="250" Margin="0,8,0,0">
                            <ListView.View>
                                <GridView>
                                    <GridViewColumn Header="Item" DisplayMemberBinding="{Binding Name}" Width="220"/>
                                    <GridViewColumn Header="Price" DisplayMemberBinding="{Binding Price}" Width="120"/>
                                </GridView>
                            </ListView.View>
                        </ListView>

                        <TextBlock Text="(Next step: Add Item dialog + Save Shopping)" Margin="0,8,0,0"/>
                    </StackPanel>
                </Grid>
            </TabItem>

            <TabItem Header="Previous Shoppings">
                <Grid Margin="10">
                    <TextBlock Text="(Next step: Load from JSON + select shopping + filter items)" />
                </Grid>
            </TabItem>
        </TabControl>
    </Grid>

</Window>
